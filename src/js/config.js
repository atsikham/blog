// local dev fallback only
// terraform renders and uploads src/js/config.js automatically for deployed environments

const CONFIG = {
  API_URL: "",   // leave empty locally to use localStorage-backed mode
};
